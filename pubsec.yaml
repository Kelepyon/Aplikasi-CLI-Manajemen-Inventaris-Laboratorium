name: lab_inventory
description: Aplikasi CLI untuk manajemen inventaris laboratorium
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  # Tidak ada dependencies eksternal — murni Dart SDK
